Jalr now works, using syscalls with JALR works
syscall is a different out than supposed JALR $zero, $k0
Solution -> Change assembler syscall? 

Confirmed works (more advanced functions):
beq, bne, jal, jr, jump

Have to use ! to mark end of taking a number in for syscall1